{
  "permissions": {
    "allow": [
      "Bash(cache_dir=\".claude/.cache/utils-agentic-support\")",
      "Bash(if [ -d \"$cache_dir/.git\" ])",
      "Bash(then git -C \"$cache_dir\" pull --ff-only)",
      "Bash(else mkdir -p .claude/.cache)",
      "Bash(git clone:*)",
      "Bash(fi)"
    ]
  }
}
